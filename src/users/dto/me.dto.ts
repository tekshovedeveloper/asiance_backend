export class MeDto {
    id: string;
    name: string;
    username: string;       // handle
    email: string;
    lastActiveText: string; // status
    bio?: string;
    avatarUrl?: string;     // avatar
    coverImageUrl?: string; // cover
  }